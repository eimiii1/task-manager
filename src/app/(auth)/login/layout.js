export const metadata = {
    title: 'Login your account | Noto',
    description: 'Pick up where you left off and stay productive.'
}

export default function LoginLayout({ children }) {
    return children
}