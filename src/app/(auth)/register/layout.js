export const metadata = {
    title: 'Create your account | Noto',
    description: 'Sign up to organize your tasks, track progress, and stay productive every day.'
}

export default function RegisterLayout({children}) {
    return children
}